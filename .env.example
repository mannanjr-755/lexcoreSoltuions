# Copy to .env.local and fill in real values. Never commit .env or .env.local.

# Neon pooled URL (host contains -pooler). Optional query params (auto-added if omitted):
# connection_limit=1 (serverless) or 5 (local), pool_timeout=30, pgbouncer=true
DATABASE_URL=
DIRECT_URL=

JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_OTP_SECRET=

APP_URL=

SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD=
SUPER_ADMIN_NAME=

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
